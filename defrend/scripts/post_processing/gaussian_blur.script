local settings = require "defrend.render.settings"
local uniforms = require "defrend.render.uniforms"

go.property("enabled", false)
go.property("separation", 1.0)

local function do_init(self)
	settings.gaussian_blur.enabled = self.enabled
	settings.gaussian_blur.separation = self.separation
	uniforms.gaussian_blur.init()
end

function init(self)
	do_init(self)
end

local MSG_REFRESH = hash("refresh")
function on_message(self, message_id, message, sender)
	if message_id == MSG_REFRESH then
		do_init(self)
	end
end

function on_reload(self)
	do_init(self)
end
