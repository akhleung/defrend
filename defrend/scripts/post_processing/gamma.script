local settings = require "defrend.render.settings"
local uniforms = require "defrend.render.uniforms"

go.property("enabled", false)
go.property("gamma", 2.2)

function init(self)
	settings.gamma.enabled	= self.enabled
	settings.gamma.gamma	= self.gamma
	uniforms.gamma.init()
end
