#version 420 core

in vec4 position;

uniform skybox_vp {
    mat4 mtx_proj;
    mat4 mtx_worldview;
};

out vec3 var_texcoord0;

void main() {
	var_texcoord0 = position.xyz;
    #ifndef EDITOR
    gl_Position = (mtx_proj * mat4(mat3(mtx_worldview)) * position).xyww; // rotate only, and clamp to the far plane
    #else
    gl_Position = mtx_proj * mtx_worldview * position;
    #endif
}
