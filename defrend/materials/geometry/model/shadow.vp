#version 320 es

in vec4 position;// positions are in world space
#ifndef EDITOR
in mat4 mtx_world;
#endif

uniform shadow_vp {
	#ifdef EDITOR
	mat4 mtx_world;
	#endif
	mat4 mtx_viewproj;
};

void main() {
	gl_Position = mtx_viewproj * mtx_world * vec4(position.xyz, 1.0);
}
